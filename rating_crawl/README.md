# Các bước chạy
1. Tạo môi trường ảo -> python -m venv .env
2. activate môi trường ảo -> .venv/scripts/activate
3. tải thư viện -> pip install -r requirements.txt
4. chạy script -> python steamdb_crawler.py